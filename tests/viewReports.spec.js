// @ts-check
import { test, expect } from '@playwright/test';

test.describe ('Tests Reports dashboard', () => {
    test.skip('Loads reports page', async({page}) => {
        //Verify Tables are present
    })
})